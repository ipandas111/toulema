[project]
name = "toulema-api"
version = "1.0.0"
requires-python = ">=3.12"

[tool.uv]
dev-dependencies = []

[tool.uv.pip-packages]
fastapi = "==0.115.0"
uvicorn = { extras = ["standard"], version = "==0.30.0" }
httpx = "==0.27.0"
python-dotenv = "==1.0.1"
anthropic = ">=0.18.0"